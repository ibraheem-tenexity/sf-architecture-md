# Demo Credentials

This app uses a demo account with no sign-in required.
The demo user is automatically active on every request.

demo_user: demo@example.com
demo_password: DemoPassword2024!

No sign-in needed — the app is pre-authenticated as the demo user.
